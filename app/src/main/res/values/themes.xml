<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TestModuleAds" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.TestModuleAds" parent="Base.Theme.TestModuleAds" />

    <!-- Tắt hiệu ứng chuyển activity cho màn native full -->
    <style name="NativeFullActivityTheme" parent="Theme.TestModuleAds">
        <item name="android:windowAnimationStyle">@null</item>
    </style>
</resources>